using System;

namespace date
{
    class Program
    {
        static void Main(string[] args)
        {
            DateTime curTime = DateTime.Now;
            Console.WriteLine(curTime);

            Console.ReadKey();
        }
    }
}
